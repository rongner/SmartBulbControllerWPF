<mah:MetroWindow x:Class="SmartBulbControllerWPF.Views.ConnectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        Title="Connect to Device"
        Width="420" Height="280"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <mah:MetroHeader Grid.Row="0" Header="IP Address" Margin="0 0 0 8">
            <TextBox Text="{Binding Ip, UpdateSourceTrigger=PropertyChanged}"
                     mah:TextBoxHelper.Watermark="e.g. 192.168.1.42" />
        </mah:MetroHeader>

        <mah:MetroHeader Grid.Row="1" Header="Device ID" Margin="0 0 0 8">
            <TextBox Text="{Binding DeviceId, UpdateSourceTrigger=PropertyChanged}"
                     mah:TextBoxHelper.Watermark="From iot.tuya.com" />
        </mah:MetroHeader>

        <mah:MetroHeader Grid.Row="2" Header="Local Key" Margin="0 0 0 8">
            <PasswordBox x:Name="LocalKeyBox" PasswordChanged="LocalKeyBox_PasswordChanged" />
        </mah:MetroHeader>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding CancelCommand}"
                    Style="{StaticResource MahApps.Styles.Button}" Width="80"
                    Margin="0 0 8 0" />
            <Button Content="Connect" Command="{Binding ConnectCommand}"
                    Style="{StaticResource MahApps.Styles.Button.Square.Accent}" Width="80" />
        </StackPanel>
    </Grid>
</mah:MetroWindow>
